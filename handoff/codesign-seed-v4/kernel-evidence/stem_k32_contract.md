# Compact-K32 stem contract

The exact scout packs 27 RGB patch values plus five zero lanes into K32 and uses an IME stem candidate with separate border handling. Float input RNE and output bytes remain exact.
