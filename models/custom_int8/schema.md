# YOLO26 Custom INT8 Model Schema

The authoritative Stage 0 schema is
`stages/BANANA-YOLO26-CUSTOM-INT8-IME-ENGINE-STAGE0-RECOVERY-AND-DESIGN-001/static_model_format_v0.md`.

No full weight blob is generated in Stage 0. Large model binaries, weights,
layer dumps, calibration dumps, and vendor binaries must stay out of git.
