# Prestage Repository State

The repository was clean at `43c02b7051ddde9921a5348e4b4e8986b941212d`
on `yolo26-k1x-resolution-sweep`. Both configured remotes resolved that source
branch to the same commit. The new branch
`yolo26-k1x-attention-ntail-r768-q0` was created from that exact point.

No pre-stage push, merge, rebase, tag, or release-branch mutation was made.
The pre-existing `/data/ncnn` worktree state was fingerprinted and treated as a
read-only invariant.
