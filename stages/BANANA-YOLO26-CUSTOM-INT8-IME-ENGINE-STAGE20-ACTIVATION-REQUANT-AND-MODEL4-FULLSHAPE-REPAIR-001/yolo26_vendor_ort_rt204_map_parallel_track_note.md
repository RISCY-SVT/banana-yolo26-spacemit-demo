# YOLO26 Vendor ORT rt204 mAP Parallel Track Note

Stage20 did not run COCO, mAP, full-image inference, camera, or vendor-ORT evaluation.

The YOLO26 vendor-ORT rt204 mAP baseline remains important for the broader project, but it should be handled as a separate parallel task. The custom engine still does not expose a full-model output path.
