# LUT-v2 plan

The next executor stage should extend the same exact resident contract to model8 while creating stable rows for 1x1, 3x3 stride1, N4/N8/N16 outputs, Add, and Concat placement. It must preserve the model4-final-to-model6 result as a regression gate and may not infer full-model latency from the model5 rate alone.
