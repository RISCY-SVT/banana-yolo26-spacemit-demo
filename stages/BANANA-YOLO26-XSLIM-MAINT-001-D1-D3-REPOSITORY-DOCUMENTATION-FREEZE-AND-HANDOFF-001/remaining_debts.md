# Remaining Debts and Non-Pass Results

- The owner audit attachments and input directory were not supplied/resolved.
  Search found none of the named audit MD/ZIP/CSV/XLSX or deferred Stage000 files.
  D1-D3 are directly reproduced against released bytes, not claimed reconciled
  with unseen reviews. A pending clarification was sent to the owner.
- Whole-tree mypy: historical DEV-002 2877 errors in 94 files, NOT freshly
  recounted. The current wider strict attempt exposes two inherited unused
  type-ignore diagnostics in unchanged yolo_output_check.py/qdq_boundary_audit.py.
  An alternate dependency interpreter experiment reports 58 errors in six
  unchanged modules. The changed three-module strict surface is zero-error.
  No global suppression or old-module repair was performed.
- Inherited REUSE per-file metadata remains incomplete. This Stage checks
  aggregate license/notices and archive hygiene, not whole-tree REUSE compliance.
- Setuptools no-match/prune notices and disabled-bytecompile notices are
  preserved. They describe exclusion/caching behavior, not changed model math.
- Native backend sdist bytes differ between clean runs. Final sdist uses the
  existing deterministic normalization packaging step, and FINAL bytes match.
  Both original backend artifacts and their different hashes are retained.
  All eight final payload files match byte-for-byte, not just after comparison
  normalization. No payload has been published.
- External URLs, future Python/platform environments and clean-network
  dependency resolution were not independently certified.
- Four external chat histories and a second owner-approved backup location
  are missing. Mirror inclusion policy for /data/ai-handoff was not proven;
  the procedural handoff therefore stays outside managed sync in private storage.
