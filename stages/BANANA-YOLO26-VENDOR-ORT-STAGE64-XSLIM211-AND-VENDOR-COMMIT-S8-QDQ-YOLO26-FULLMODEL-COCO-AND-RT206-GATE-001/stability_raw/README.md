# Raw stability evidence

The 1,000-run and 10,000-run sample streams remain on NVMe. The committed
long-soak table records distribution statistics and output-hash cardinality;
raw runner output is not copied into Git or the sanitized result packet.
