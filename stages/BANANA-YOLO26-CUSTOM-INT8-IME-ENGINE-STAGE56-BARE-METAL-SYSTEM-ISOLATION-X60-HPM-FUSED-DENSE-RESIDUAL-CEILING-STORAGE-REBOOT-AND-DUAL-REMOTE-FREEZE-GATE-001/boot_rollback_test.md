# Boot rollback test

Three B0 reboot/SSH/benchmark cycles succeeded. Original boot files and hashes match the backup manifest. No boot setting required rollback because no alternate profile was written.
