# Alternate kernel

No alternate kernel was built. Runtime O2 delivered a selected improvement, while the vendor boot surface exposed no recovery-safe second entry for a conditional kernel/DTB. The accepted kernel and Device Tree remain unchanged.
