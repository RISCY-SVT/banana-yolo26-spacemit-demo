# Bootloader inventory

The board uses the vendor U-Boot/boot-script path on the eMMC-backed `/boot` tree with one validated boot entry. No recovery-safe B1-B3 alternate entry was available; the original entry was retained and exercised across three B0 cold boots.
