# vmadotn Recheck Final Note

stage_id: BANANA-YOLO26-CUSTOM-INT8-IME-ENGINE-STAGE32-MMT4D-OR-LOW-OVERHEAD-SLIDING-DATA-LAYOUT-DECISION-001

Stage31 already tried the visible candidate mnemonic forms:

```text
smt.vmadotn
smt.vmadot.n
smt.vmadot4
```

The assembler rejected them in the current proven toolchain route.

Stage32 did not perform raw opcode probing. That remains forbidden without a source-backed opcode encoding and an explicit safety plan. `vmadotn` remains not authorized and not integrated.
