# Track B Vendor ORT rt204 mAP Baseline TODO

Stage26 did not run mAP.

A separate first-class task is still needed:

```text
BANANA-YOLO26-VENDOR-ORT-RT204-MAP-BASELINE-001
```

Purpose:

```text
Measure YOLO26 INT8 or best available YOLO26 runtime path mAP on vendor SpacemiT ORT rt204 using a fixed COCO validation protocol, and compare carefully to YOLO11 production numbers without mixing model/runtime/precision claims.
```

This Track B task must remain separate from the custom-engine selected-cut Stage26 work.
