# Final dual-remote publication

Before optimization, both `github/yolo26-custom-int8-engine` and
`gitlab-rd/yolo26-custom-int8-engine` were verified at accepted Stage52 HEAD
`c98212b6db56a7a27a0d26b645a7158dc36c5ff6` after a normal fast-forward push
where required.

Stage53 produced four exact implementation, validation, release, and evidence
commits through `b84c9dfbbefdf7003df998d7e2ec510d595d6358`. This report and the
remaining publication tables are stored by the immediately following
`containing-commit` whose parent is that evidence commit.

The containing commit is eligible for publication only after both fetched
remote branch heads pass an ancestor check. Publication uses normal pushes to
`refs/heads/yolo26-custom-int8-engine`; force push, rebase, automatic merge,
tag, release, and pull-request operations are excluded.

A tracked file cannot contain the object ID of the Git commit that contains
that file. The exact containing-commit SHA and post-push GitHub/GitLab
parity are therefore recorded in the immutable command ledger, official result
packet, and final console response after both pushes complete.
