# Stage56 Pipeline Surface Errata

Serial pipeline results must state preprocessor CPU placement and OpenCV thread count. Double-buffer interval is throughput, not pure-model latency. Stage55 and Stage56 serial pipeline rows are not a pure executor A/B.
