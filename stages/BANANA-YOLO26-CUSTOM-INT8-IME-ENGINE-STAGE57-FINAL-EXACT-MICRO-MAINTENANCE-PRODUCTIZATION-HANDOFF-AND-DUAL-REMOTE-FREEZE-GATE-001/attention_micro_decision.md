# Attention Micro Decision

MatMul C8 selected (-2.773859374% mean, CI high -3432.595300 us). Softmax cache rejected (0.427840794% mean); exactness alone did not override the full-model selection rule.
