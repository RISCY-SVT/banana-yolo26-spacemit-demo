# O2 Handoff Decision

O2 retained as the optional `low-latency-dedicated` wrapper. Apply, run, EXIT, INT, TERM, HUP, timeout, command failure, invalid package, nested invocation, and stale recovery gates restored the original state.
