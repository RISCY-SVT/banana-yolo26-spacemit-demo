# Release Source Dependency Graph

`y26_k1x_executor.h` -> C ABI -> full executor -> package loader/integer contract -> persistent core/Q62/IME kernels. CLI and healthcheck link only the ABI1 shared target. Historical runners and rejected kernels are research-only.
