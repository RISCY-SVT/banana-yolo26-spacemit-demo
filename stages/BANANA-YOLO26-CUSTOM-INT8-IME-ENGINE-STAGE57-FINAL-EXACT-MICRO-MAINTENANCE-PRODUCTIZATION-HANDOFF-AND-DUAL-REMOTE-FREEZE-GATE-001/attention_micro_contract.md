# Attention Micro Contract

The MatMul C8 path combines neighboring C4 correction groups and applies exact Q62 without a scalar corrected[4] construction. Existing direct second-MatMul packing remains selected.

The generation-cache scout retained exact unsigned-128 normalization but was rejected by full-model wall time.
