# Model/Executor Co-design Handoff

Status: `reference-only; execution-not-authorized`.

The vendor PTQ, provider-numerics and current fusion lanes are closed. A future separately authorized effort may test symmetric-S8 `K1X_INT8_V2` as a causal hypothesis using one source model across export/runtime surfaces.

Such work must preserve B2 as the universal vendor control, bind C2 only as the waived higher-AP profile, define application TP/FP/FN costs before candidate selection, and use an untouched final holdout. It must separate model, quantizer, graph, executor and runtime effects and must not reinterpret `K1X_INT8_V1`.

No prompt, branch, model conversion, executor change, board run, QAT or co-design experiment is created by DEV-002.
