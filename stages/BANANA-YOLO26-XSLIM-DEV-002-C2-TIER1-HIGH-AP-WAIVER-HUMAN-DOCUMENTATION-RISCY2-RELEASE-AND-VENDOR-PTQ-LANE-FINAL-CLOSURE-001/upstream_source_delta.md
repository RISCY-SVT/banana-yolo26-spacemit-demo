# Upstream Source Delta

Fetched upstream `main` is exactly commit `9a33f2f770d00fd02ff8bc0f1907135e9bf47f8c`, tree `05d2c8425ab8587abf401fa5976a08d008fdd719`, which is the accepted downstream base. The commit count from that base to current upstream `main` is zero.

Thirteen live upstream branch refs and 47 pull-request records were inventoried. The YoloDecode and YOLO26 Reduce lines are historical branch surfaces whose merged capabilities already exist in the accepted base. Other unmerged FP16, L2, graph-pass, package and workflow branches are either irrelevant to this release or semantic changes requiring a separate authorization.

No upstream commit was merged or cherry-picked. No security- or correctness-critical semantic delta blocks release `2.1.2+riscy.2`.
