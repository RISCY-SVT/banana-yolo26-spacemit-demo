# Cross-surface comparison caveats

The accepted custom executor and vendor B2/C2 rows use different source/export/quantization/runtime surfaces. Their accuracy and same-boot timing are application-level context only, not an engine-only, quantizer-only, or same-source backend comparison. No camera surface was run.
