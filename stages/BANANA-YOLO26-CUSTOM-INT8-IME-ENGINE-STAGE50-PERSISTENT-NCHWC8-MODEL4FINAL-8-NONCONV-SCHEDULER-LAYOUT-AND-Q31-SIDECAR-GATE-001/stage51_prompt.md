# Stage 51 prompt: full-graph coverage estimator and next resident-region gate

Use Stage 50 as accepted evidence that the exact resident K1X_INT8_V1 model4-final-to-model8 region is strongly faster than the equivalent B120 ORT cut. Do not claim a full engine or 20 FPS.

1. Freeze the Stage 50 package, binary, correctness, and timing identities.
2. Census the remaining exact graph from input through output and map every MAC and material non-MAC node to stable LUT-v2 rows or explicit unsupported rows.
3. Build optimistic, central, and conservative full-graph executor estimates with cache, arena, scheduling, and head costs stated explicitly.
4. Select exactly one next resident region based on uncovered measured cost and layout compatibility; do not expand block-by-block by default.
5. Preserve K1X_INT8_V1 and NCHWc8 as mainline. Q31, Q62 E2b, and alternative layouts remain sidecars until their own gates pass.
6. Keep student 416/512 held and training unauthorized until graph coverage and an explicit accuracy target are decision-ready.
