# PMU report

Per-worker basic counters were available after affinity. The instrumented model4-final to model8 run reported 167028822 cycles and 137760624 instructions across workers, aggregate IPC 0.824771571. The scope is an instrumented worker aggregate, not a headline kernel IPC. Generic cache events returned `time_running=0` and are classified unsupported, not zero activity. Stable wall time remains the selection authority.
