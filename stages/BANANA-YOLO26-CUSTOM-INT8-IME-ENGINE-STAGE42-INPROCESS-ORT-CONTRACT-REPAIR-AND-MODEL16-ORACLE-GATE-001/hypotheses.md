# Hypotheses

H1: Stage41 host scalar exactness reproduces under an explicit ORT session contract.

H2: The board ORT mismatch persists under identical model/input bytes and `ORT_DISABLE_ALL`, making a fixed-host-oracle policy more likely than a matched-runtime policy.

H3: Identical saved model4 input bytes separate prefix drift from ORT block drift and scalar/IME route differences.

H4: Removing the model4 ORT reference from the measured custom loop materially lowers and fully attributes custom-pipeline timing.

H5: A reproducible fixed host oracle is sufficient to generate a model16 oracle package without authorizing optimized model16 code.
