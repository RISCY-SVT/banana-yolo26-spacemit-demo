# System Rollback Report

Every O2 and camera IRQ profile reported inactive after normal, signal, capture-failure, and recorder-failure paths. No persistent boot, governor, sysctl, cgroup, IRQ, workqueue, or service policy was selected.
